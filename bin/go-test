#!/bin/bash

set -eu

if [ $# -ne 1 ]; then
    echo "Usage: bin/${0##*/} [package to test] [Test ID]" >&2
    exit
fi

go test -v ./tests/$1 -ginkgo.v -ginkgo.reportFile=$(pwd)/reports/$2-$1.junit.xml
